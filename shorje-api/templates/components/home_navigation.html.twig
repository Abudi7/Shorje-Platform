{# Home Navigation Component - Reusable across all pages #}
<!-- Modern Arabic Marketplace Header -->
<nav class="glass sticky top-0 z-40 border-b border-white/20 dark:border-slate-700/30">
    <!-- Top Promotional Bar - Responsive -->
    <div class="bg-gradient-to-r from-emerald-600 via-blue-600 to-purple-600 text-white text-center py-1.5 sm:py-2 md:py-3 text-xs sm:text-sm relative overflow-hidden">
        <div class="absolute inset-0 bg-gradient-to-r from-transparent via-white/10 to-transparent animate-pulse"></div>
        <div class="max-w-7xl mx-auto px-1 sm:px-2 md:px-4 relative">
            <div class="flex items-center justify-center space-x-1 sm:space-x-2 md:space-x-4">
                <span class="font-bold text-xs sm:text-sm md:text-lg animate-bounce">🎉</span>
                <span class="font-bold hidden md:inline">{{ 'app.hero_description'|trans }}</span>
                <span class="font-bold hidden sm:inline md:hidden">{{ 'app.hero_subtitle'|trans }}</span>
                <span class="font-bold sm:hidden">{{ 'app.hero_title'|trans }}</span>
                <span class="font-bold text-xs sm:text-sm md:text-lg animate-bounce">🎉</span>
            </div>
        </div>
    </div>
    
    <!-- Main Header - Responsive -->
    <div class="bg-white/90 dark:bg-slate-900/90 backdrop-blur-sm relative z-50">
        <div class="max-w-7xl mx-auto px-2 sm:px-4 md:px-6 lg:px-8">
            <div class="flex items-center justify-between h-14 sm:h-16 md:h-18 lg:h-20">
                <!-- Logo Section -->
                <div class="flex items-center">
                    <div class="flex items-center space-x-1 sm:space-x-2 md:space-x-3 lg:space-x-4">
                        <div class="h-8 w-8 sm:h-10 sm:w-10 md:h-11 md:w-11 lg:h-12 lg:w-12 bg-gradient-to-br from-emerald-500 via-blue-500 to-purple-500 rounded-lg sm:rounded-xl md:rounded-2xl flex items-center justify-center shadow-lg relative overflow-hidden">
                            <div class="absolute inset-0 bg-gradient-to-br from-white/20 to-transparent"></div>
                            <i class="fas fa-store text-white text-sm sm:text-lg md:text-xl relative z-10"></i>
                        </div>
                        <div>
                            <h1 class="text-lg sm:text-xl md:text-2xl lg:text-3xl font-bold gradient-text">{{ 'app.platform_name'|trans }}</h1>
                            <p class="text-xs sm:text-sm text-gray-600 dark:text-gray-400 hidden sm:block">{{ 'app.description'|trans }}</p>
                        </div>
                    </div>
                </div>

                <!-- User Actions - Responsive -->
                <div class="flex items-center space-x-2 sm:space-x-3 md:space-x-4">
                    {% if app.user %}
                    <!-- User Profile Dropdown -->
                    <div class="relative" id="userDropdown">
                        <button onclick="toggleUserDropdown()" class="flex items-center space-x-2 sm:space-x-3 bg-white dark:bg-slate-800 px-2 sm:px-3 py-1 sm:py-2 rounded-lg sm:rounded-xl hover:shadow-lg transition-all duration-300 border border-gray-200 dark:border-slate-600">
                            <div class="h-8 w-8 sm:h-10 sm:w-10 bg-gradient-to-br from-emerald-400 to-blue-500 rounded-full flex items-center justify-center overflow-hidden shadow-md ring-2 ring-white dark:ring-slate-800">
                                <img src="/api/user/avatar/{{ app.user.id }}" 
                                     alt="{{ app.user.firstName }} {{ app.user.lastName }}" 
                                     class="w-full h-full object-cover"
                                     onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';">
                                <i class="fas fa-user text-white text-xs sm:text-sm" style="display: none;"></i>
                            </div>
                            <div class="text-right hidden lg:block">
                                <p class="text-xs sm:text-sm font-semibold text-gray-900 dark:text-white">{{ app.user.firstName }} {{ app.user.lastName }}</p>
                                <p class="text-xs text-gray-500 dark:text-gray-400">{{ 'app.welcome'|trans }}</p>
                            </div>
                            <i class="fas fa-chevron-down text-gray-400 dark:text-gray-500 text-xs sm:text-sm transition-transform duration-300" id="dropdownArrow"></i>
                        </button>
                        
                        <!-- Dropdown Menu - Responsive -->
                        <div id="userDropdownMenu" class="absolute top-full right-0 mt-2 sm:mt-3 w-64 sm:w-72 md:w-80 lg:w-72 xl:w-80 bg-white dark:bg-slate-800 rounded-xl sm:rounded-2xl shadow-2xl border border-gray-200 dark:border-slate-700 opacity-0 invisible scale-95 transition-all duration-300 z-[9999] overflow-hidden">
                            <!-- User Info Header with Gradient -->
                            <div class="bg-gradient-to-r from-emerald-500 via-blue-500 to-purple-500 p-2 sm:p-3 md:p-4">
                                <div class="flex items-center space-x-2 sm:space-x-3">
                                    <div class="h-10 w-10 sm:h-12 sm:w-12 md:h-14 md:w-14 bg-white dark:bg-slate-800 rounded-full flex items-center justify-center overflow-hidden shadow-lg ring-2 sm:ring-4 ring-white/30">
                                        <img src="/api/user/avatar/{{ app.user.id }}" 
                                             alt="{{ app.user.firstName }} {{ app.user.lastName }}" 
                                             class="w-full h-full object-cover"
                                             onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';">
                                        <i class="fas fa-user text-emerald-500 text-xs sm:text-sm md:text-lg" style="display: none;"></i>
                                    </div>
                                    <div class="flex-1">
                                        <p class="font-bold text-white text-xs sm:text-sm md:text-base">{{ app.user.firstName }} {{ app.user.lastName }}</p>
                                        <p class="text-xs sm:text-sm text-white/80 truncate">{{ app.user.email }}</p>
                                    </div>
                                </div>
                            </div>
                            
                            <!-- Menu Items -->
                            <div class="p-1 sm:p-2">
                                <a href="/profile" class="flex items-center space-x-2 sm:space-x-3 px-2 sm:px-3 md:px-4 py-2 sm:py-3 text-gray-700 dark:text-gray-300 hover:bg-gradient-to-r hover:from-emerald-50 hover:to-blue-50 dark:hover:from-emerald-900/20 dark:hover:to-blue-900/20 rounded-lg sm:rounded-xl transition-all group">
                                    <div class="w-7 h-7 sm:w-8 sm:h-8 md:w-10 md:h-10 bg-gradient-to-br from-emerald-400 to-blue-500 rounded-lg flex items-center justify-center shadow-md group-hover:scale-110 transition-transform">
                                        <i class="fas fa-user text-white text-xs sm:text-sm"></i>
                                    </div>
                                    <span class="font-medium text-xs sm:text-sm md:text-base">{{ 'navigation.profile'|trans }}</span>
                                </a>
                                <a href="/products/my" class="flex items-center space-x-2 sm:space-x-3 px-2 sm:px-3 md:px-4 py-2 sm:py-3 text-gray-700 dark:text-gray-300 hover:bg-gradient-to-r hover:from-purple-50 hover:to-pink-50 dark:hover:from-purple-900/20 dark:hover:to-pink-900/20 rounded-lg sm:rounded-xl transition-all group">
                                    <div class="w-7 h-7 sm:w-8 sm:h-8 md:w-10 md:h-10 bg-gradient-to-br from-purple-400 to-pink-500 rounded-lg flex items-center justify-center shadow-md group-hover:scale-110 transition-transform">
                                        <i class="fas fa-box text-white text-xs sm:text-sm"></i>
                                    </div>
                                    <span class="font-medium text-xs sm:text-sm md:text-base">{{ 'navigation.my_products'|trans }}</span>
                                </a>
                                <a href="/saved" class="flex items-center space-x-2 sm:space-x-3 px-2 sm:px-3 md:px-4 py-2 sm:py-3 text-gray-700 dark:text-gray-300 hover:bg-gradient-to-r hover:from-red-50 hover:to-pink-50 dark:hover:from-red-900/20 dark:hover:to-pink-900/20 rounded-lg sm:rounded-xl transition-all group">
                                    <div class="w-7 h-7 sm:w-8 sm:h-8 md:w-10 md:h-10 bg-gradient-to-br from-red-400 to-pink-500 rounded-lg flex items-center justify-center shadow-md group-hover:scale-110 transition-transform">
                                        <i class="fas fa-heart text-white text-xs sm:text-sm"></i>
                                    </div>
                                    <span class="font-medium text-xs sm:text-sm md:text-base">{{ 'navigation.saved'|trans }}</span>
                                </a>
                                <a href="/help" class="flex items-center space-x-2 sm:space-x-3 px-2 sm:px-3 md:px-4 py-2 sm:py-3 text-gray-700 dark:text-gray-300 hover:bg-gradient-to-r hover:from-orange-50 hover:to-yellow-50 dark:hover:from-orange-900/20 dark:hover:to-yellow-900/20 rounded-lg sm:rounded-xl transition-all group">
                                    <div class="w-7 h-7 sm:w-8 sm:h-8 md:w-10 md:h-10 bg-gradient-to-br from-orange-400 to-yellow-500 rounded-lg flex items-center justify-center shadow-md group-hover:scale-110 transition-transform">
                                        <i class="fas fa-question-circle text-white text-xs sm:text-sm"></i>
                                    </div>
                                    <span class="font-medium text-xs sm:text-sm md:text-base">{{ 'navigation.how_it_works'|trans }}</span>
                                </a>
                                <a href="/contact" class="flex items-center space-x-2 sm:space-x-3 px-2 sm:px-3 md:px-4 py-2 sm:py-3 text-gray-700 dark:text-gray-300 hover:bg-gradient-to-r hover:from-green-50 hover:to-emerald-50 dark:hover:from-green-900/20 dark:hover:to-emerald-900/20 rounded-lg sm:rounded-xl transition-all group">
                                    <div class="w-7 h-7 sm:w-8 sm:h-8 md:w-10 md:h-10 bg-gradient-to-br from-green-400 to-emerald-500 rounded-lg flex items-center justify-center shadow-md group-hover:scale-110 transition-transform">
                                        <i class="fas fa-envelope text-white text-xs sm:text-sm"></i>
                                    </div>
                                    <span class="font-medium text-xs sm:text-sm md:text-base">{{ 'navigation.contact'|trans }}</span>
                                </a>
                                
                                {% if app.user and ('ROLE_ADMIN' in app.user.roles or 'ROLE_SUPER_ADMIN' in app.user.roles) %}
                                <div class="my-1 sm:my-2 border-t border-gray-200 dark:border-slate-600"></div>
                                <a href="/admin" class="flex items-center space-x-2 sm:space-x-3 px-2 sm:px-3 md:px-4 py-2 sm:py-3 text-gray-700 dark:text-gray-300 hover:bg-gradient-to-r hover:from-purple-50 hover:to-indigo-50 dark:hover:from-purple-900/20 dark:hover:to-indigo-900/20 rounded-lg sm:rounded-xl transition-all group">
                                    <div class="w-7 h-7 sm:w-8 sm:h-8 md:w-10 md:h-10 bg-gradient-to-br from-purple-500 to-indigo-600 rounded-lg flex items-center justify-center shadow-md group-hover:scale-110 transition-transform">
                                        <i class="fas fa-cog text-white text-xs sm:text-sm"></i>
                                    </div>
                                    <span class="font-medium text-xs sm:text-sm md:text-base">{{ 'navigation.settings'|trans }}</span>
                                </a>
                                {% endif %}
                                
                                <div class="my-1 sm:my-2 border-t border-gray-200 dark:border-slate-600"></div>
                                <a href="/logout" class="flex items-center space-x-2 sm:space-x-3 px-2 sm:px-3 md:px-4 py-2 sm:py-3 text-gray-700 dark:text-gray-300 hover:bg-gradient-to-r hover:from-red-50 hover:to-orange-50 dark:hover:from-red-900/20 dark:hover:to-orange-900/20 rounded-lg sm:rounded-xl transition-all group">
                                    <div class="w-7 h-7 sm:w-8 sm:h-8 md:w-10 md:h-10 bg-gradient-to-br from-red-500 to-orange-500 rounded-lg flex items-center justify-center shadow-md group-hover:scale-110 transition-transform">
                                        <i class="fas fa-sign-out-alt text-white text-xs sm:text-sm"></i>
                                    </div>
                                    <span class="font-medium text-xs sm:text-sm md:text-base">{{ 'navigation.logout'|trans }}</span>
                                </a>
                            </div>
                        </div>
                    </div>
                    {% else %}
                    <!-- Guest Actions -->
                    <div class="flex items-center space-x-3">
                        <a href="/login" class="text-gray-700 dark:text-gray-300 hover:text-emerald-600 dark:hover:text-emerald-400 font-medium px-4 py-2 rounded-xl hover:bg-gray-100 dark:hover:bg-slate-700 transition-all duration-300">
                            <i class="fas fa-sign-in-alt {{ app.request.locale == 'ar' ? 'ml-2' : 'mr-2' }}"></i>{{ 'navigation.login'|trans }}
                        </a>
                        <a href="/register" class="bg-gradient-to-r from-emerald-500 to-blue-500 text-white px-6 py-2 rounded-xl font-medium hover:from-emerald-600 hover:to-blue-600 transition-all duration-300 shadow-lg hover:shadow-xl">
                            <i class="fas fa-user-plus {{ app.request.locale == 'ar' ? 'ml-2' : 'mr-2' }}"></i>{{ 'navigation.register'|trans }}
                        </a>
                    </div>
                    {% endif %}
                    
                    <!-- Theme Toggle -->
                    <div class="relative">
                        <button onclick="toggleTheme()" id="themeToggle" class="relative p-2.5 text-gray-600 dark:text-gray-300 hover:bg-gray-100 dark:hover:bg-slate-700 transition-all duration-200 rounded-lg group" title="تبديل الوضع">
                            <i class="fas fa-sun text-xl hidden dark:block text-yellow-400 group-hover:rotate-180 transition-transform duration-300" id="lightIcon"></i>
                            <i class="fas fa-moon text-xl block dark:hidden text-indigo-600 group-hover:-rotate-12 transition-transform duration-300" id="darkIcon"></i>
                        </button>
                    </div>
                    
                    <!-- Messages -->
                    <div class="relative">
                        <a href="/messages" class="relative p-2.5 text-gray-600 dark:text-gray-300 hover:bg-gray-100 dark:hover:bg-slate-700 transition-all duration-200 rounded-lg">
                            <i class="fas fa-comments text-xl"></i>
                            <span id="messagesCount" class="absolute -top-1 -right-1 bg-gradient-to-r from-red-500 to-pink-500 text-white text-xs rounded-full h-5 w-5 flex items-center justify-center hidden shadow-lg animate-pulse">0</span>
                        </a>
                    </div>
                    
                    <!-- Language Switcher -->
                    <div class="relative" id="languageSwitcherContainer">
                        <button onclick="toggleLanguageSwitcher()" class="relative p-2.5 text-gray-600 dark:text-gray-300 hover:bg-gray-100 dark:hover:bg-slate-700 transition-all duration-200 rounded-lg group" title="{{ 'language.change_language'|trans }}">
                            <i class="fas fa-language text-xl group-hover:rotate-12 transition-transform duration-300"></i>
                            <span class="ml-1 text-xs font-bold">{{ app.request.locale|upper }}</span>
                        </button>
                        
                        <!-- Language Dropdown -->
                        <div id="languageDropdown" class="absolute {{ app.request.locale == 'ar' ? 'right-0' : 'left-0' }} mt-3 w-56 bg-white dark:bg-slate-800 rounded-2xl shadow-2xl border border-gray-200 dark:border-slate-700 z-[9999] hidden overflow-hidden">
                            <div class="p-2">
                                <a href="{{ path('app_change_language', {'locale': 'ar'}) }}" class="flex items-center space-x-3 px-4 py-3 text-gray-700 dark:text-gray-300 hover:bg-gradient-to-r hover:from-emerald-50 hover:to-blue-50 dark:hover:from-emerald-900/20 dark:hover:to-blue-900/20 rounded-xl transition-all group {{ app.request.locale == 'ar' ? 'bg-emerald-50 dark:bg-emerald-900/20' : '' }}">
                                    <div class="w-10 h-10 bg-gradient-to-br from-emerald-400 to-green-500 rounded-lg flex items-center justify-center shadow-md group-hover:scale-110 transition-transform text-2xl">
                                        🇸🇦
                                    </div>
                                    <div class="flex-1">
                                        <p class="font-semibold text-sm">{{ 'language.arabic'|trans }}</p>
                                        <p class="text-xs text-gray-500 dark:text-gray-400">Arabic</p>
                                    </div>
                                    {% if app.request.locale == 'ar' %}
                                    <i class="fas fa-check text-emerald-500"></i>
                                    {% endif %}
                                </a>
                                <a href="{{ path('app_change_language', {'locale': 'en'}) }}" class="flex items-center space-x-3 px-4 py-3 text-gray-700 dark:text-gray-300 hover:bg-gradient-to-r hover:from-blue-50 hover:to-indigo-50 dark:hover:from-blue-900/20 dark:hover:to-indigo-900/20 rounded-xl transition-all group {{ app.request.locale == 'en' ? 'bg-blue-50 dark:bg-blue-900/20' : '' }}">
                                    <div class="w-10 h-10 bg-gradient-to-br from-blue-400 to-indigo-500 rounded-lg flex items-center justify-center shadow-md group-hover:scale-110 transition-transform text-2xl">
                                        🇺🇸
                                    </div>
                                    <div class="flex-1">
                                        <p class="font-semibold text-sm">{{ 'language.english'|trans }}</p>
                                        <p class="text-xs text-gray-500 dark:text-gray-400">الإنجليزية</p>
                                    </div>
                                    {% if app.request.locale == 'en' %}
                                    <i class="fas fa-check text-blue-500"></i>
                                    {% endif %}
                                </a>
                            </div>
                        </div>
                    </div>
                    
                    <!-- Notification Dropdown -->
                    {% include 'components/notification_dropdown.html.twig' %}
                </div>
            </div>
        </div>
    </div>

    <!-- Navigation Menu -->
    <div class="bg-white/80 dark:bg-slate-800/80 backdrop-blur-sm border-t border-gray-200/50 dark:border-slate-600/50 relative z-10">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex items-center justify-between py-4">
                <div class="flex items-center space-x-8">
                    <a href="/home" class="flex items-center space-x-2 text-gray-700 dark:text-gray-300 hover:text-emerald-600 dark:hover:text-emerald-400 font-medium transition-colors">
                        <i class="fas fa-home"></i>
                        <span>{{ 'navigation.home'|trans }}</span>
                    </a>
                    <a href="/products" class="flex items-center space-x-2 text-gray-700 dark:text-gray-300 hover:text-emerald-600 dark:hover:text-emerald-400 font-medium transition-colors">
                        <i class="fas fa-th-large"></i>
                        <span>{{ 'navigation.products'|trans }}</span>
                    </a>
                    <a href="/users" class="flex items-center space-x-2 text-gray-700 dark:text-gray-300 hover:text-emerald-600 dark:hover:text-emerald-400 font-medium transition-colors">
                        <i class="fas fa-users"></i>
                        <span>{{ 'navigation.sellers'|trans }}</span>
                    </a>
                    {% if app.user %}
                    <a href="/products/my" class="flex items-center space-x-2 text-gray-700 dark:text-gray-300 hover:text-emerald-600 dark:hover:text-emerald-400 font-medium transition-colors">
                        <i class="fas fa-box text-purple-500"></i>
                        <span>{{ 'navigation.my_products'|trans }}</span>
                    </a>
                    {% endif %}
                    {% if app.user and 'ROLE_SUPER_ADMIN' in app.user.roles %}
                    <a href="/admin" class="flex items-center space-x-2 text-gray-700 dark:text-gray-300 hover:text-red-600 dark:hover:text-red-400 font-medium transition-colors bg-red-50 dark:bg-red-900/20 px-3 py-2 rounded-lg">
                        <i class="fas fa-crown text-red-500"></i>
                        <span>{{ 'navigation.settings'|trans }}</span>
                    </a>
                    {% endif %}
                </div>
            </div>
        </div>
    </div>
</nav>

<script>
// Navigation functionality
document.addEventListener('DOMContentLoaded', function() {
    // User dropdown
    const userDropdown = document.getElementById('userDropdown');
    const userDropdownMenu = document.getElementById('userDropdownMenu');
    const dropdownArrow = document.getElementById('dropdownArrow');
    
    if (userDropdown && userDropdownMenu) {
        // Close dropdown when clicking outside
        document.addEventListener('click', function(event) {
            if (!userDropdown.contains(event.target)) {
                userDropdownMenu.classList.add('opacity-0', 'invisible', 'scale-95');
                userDropdownMenu.classList.remove('opacity-100', 'visible', 'scale-100');
                if (dropdownArrow) dropdownArrow.classList.remove('rotate-180');
            }
        });
    }
    
    // Initialize theme
    initTheme();
    
    // Close language dropdown when clicking outside
    document.addEventListener('click', function(event) {
        const languageContainer = document.getElementById('languageSwitcherContainer');
        const languageDropdown = document.getElementById('languageDropdown');
        if (languageContainer && languageDropdown && !languageContainer.contains(event.target)) {
            languageDropdown.classList.add('hidden');
        }
    });
});

function toggleUserDropdown() {
    const dropdown = document.getElementById('userDropdownMenu');
    const arrow = document.getElementById('dropdownArrow');
    
    if (dropdown.classList.contains('opacity-0')) {
        dropdown.classList.remove('opacity-0', 'invisible', 'scale-95');
        dropdown.classList.add('opacity-100', 'visible', 'scale-100');
        if (arrow) arrow.classList.add('rotate-180');
    } else {
        dropdown.classList.add('opacity-0', 'invisible', 'scale-95');
        dropdown.classList.remove('opacity-100', 'visible', 'scale-100');
        if (arrow) arrow.classList.remove('rotate-180');
    }
}

function toggleLanguageSwitcher() {
    const dropdown = document.getElementById('languageDropdown');
    dropdown.classList.toggle('hidden');
}

function toggleTheme() {
    const html = document.documentElement;
    const currentTheme = localStorage.getItem('theme') || 'light';
    const newTheme = currentTheme === 'dark' ? 'light' : 'dark';
    
    // Toggle dark class
    if (newTheme === 'dark') {
        html.classList.add('dark');
    } else {
        html.classList.remove('dark');
    }
    
    // Save preference
    localStorage.setItem('theme', newTheme);
}

function initTheme() {
    const savedTheme = localStorage.getItem('theme');
    const prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
    const theme = savedTheme || (prefersDark ? 'dark' : 'light');
    
    if (theme === 'dark') {
        document.documentElement.classList.add('dark');
    } else {
        document.documentElement.classList.remove('dark');
    }
    
    // Save initial theme if not set
    if (!savedTheme) {
        localStorage.setItem('theme', theme);
    }
}

function openMessages() {
    window.location.href = '/messages';
}
</script>

